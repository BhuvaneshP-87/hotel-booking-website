pipeline{
    agent any
    tools {nodejs "node"}

    stages{
        stage('Compile Stage'){
            steps{
                sh 'npm i package.json'
                sh 'nodemon app.js'
            }
        }

        stage('Testing'){
            steps{
                sh 'npm test'
            }
        }
    }
}