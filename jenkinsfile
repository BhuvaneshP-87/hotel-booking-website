pipeline{
    agent any
    tools {nodejs "Ease-Rooms"}

    stages{
        stage('Compile Stage'){
            steps{
                sh 'npm install'
                sh 'npm i package.json'
                sh 'node app.js'
            }
        }

        stage('Testing'){
            steps{
                sh 'node test'
            }
        }
    }
}